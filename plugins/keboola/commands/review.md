---
description: Code quality + backward compatibility review for Keboola Python components
allowed-tools: Read, Glob, Grep, Bash
argument-hint: [paths-or-scope]
---

Review this Keboola component using the `component-developer:review` skill.

Scope: use `$ARGUMENTS` if provided (specific files, PR ref, or branch). Otherwise review unstaged changes via `git diff`.
